#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 *@a : first integer
 *@b : second integer
 *return: always 0
 */

int mul(int a, int b)
{
	int prod;

	prod = a * b;
	return (prod);
}
